# Backend (API + persistencia)

Componentes backend en este monorepo:

- `api/`: endpoints serverless (Vercel).
- `lib/`: servicios compartidos de datos.
- `server.mjs`: servidor local para desarrollo full-stack.
- `supabase/`: scripts y configuración de datos.

Se mantiene en raíz para compatibilidad con el despliegue actual.
